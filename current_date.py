from datetime import date, timedelta, datetime

current = date.today()

def advance_time(num_of_days):
    # current += timedelta(days=2)
    return None

def reverse_time(num_of_days):
    # current -= timedelta(days=2)
    return None

def reset_time():
    # current = date.today()
    return None